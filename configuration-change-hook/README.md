# Configuration Change Hook

A Git `post-receive` hook that publishes the `tGallery/onConfigurationChange` event when a push is accepted into the repo.

## Usage

The hook is installed in the container and can be used in a repo by creating a symlink, see [parent README](../README.md).
